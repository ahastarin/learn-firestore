{
  "name": "learn-firestore",
  "version": "1.0.0",
  "description": "All course files for the Firebase Firestore playlist on The Net Ninja YouTube channel.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase": "^10.11.1",
    "nodemon": "^3.1.0"
  }
}
